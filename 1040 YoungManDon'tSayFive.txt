#include<bits/stdc++.h>
using namespace std;
int main()
{
    char ch;
    while((ch=getchar())!='\n')
    {
        if (ch=='5')
            cout<<'*';
        else
            cout<<ch;
    }
    return 0;
}